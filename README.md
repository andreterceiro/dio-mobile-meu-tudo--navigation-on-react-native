# Introduction

when we use the following command **as example**:

```
expo init react-nav --npm
```

I created a project with the above command selecting the template "Blank (TypeScript)" in the directory tests\01.
